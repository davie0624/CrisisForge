# Notebooks

Notebooks are reader-facing, executed companions. Core logic lives in importable
Python modules under `src/crisisforge/`; notebooks must not contain an alternative
hidden implementation.
